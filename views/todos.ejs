<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Todo List</title>
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #121212;
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }
    .container {
      background: #1e1e1e;
      padding: 30px 40px;
      border-radius: 12px;
      width: 500px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
      margin-top: 40px;
      margin-bottom: 20px;
    }
    h1 {
      text-align: center;
      margin-bottom: 20px;
    }
    form {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
    }
    input[type="text"], input[type="date"] {
      flex: 1;
      padding: 10px;
      background: #2c2c2c;
      border: none;
      color: #fff;
      border-radius: 6px;
    }
    button {
      background-color: #4e9af1;
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    button:hover {
      background-color: #2a88ff;
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    li {
      background-color: #2a2a2a;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .task-text {
      flex: 1;
    }
    .due-date {
      font-size: 0.9em;
      color: #bbb;
    }
    .actions form {
      display: inline-block;
      margin-left: 5px;
    }
    footer {
      text-align: center;
      color: #aaa;
      font-size: 0.9em;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>📝 Todo List</h1>

    <form action="/todos/add" method="POST">
      <input type="text" name="task" placeholder="New task" required>
      <input type="date" name="dueDate">
      <button type="submit">Add</button>
    </form>

    <ul>
      <% todos.forEach(todo => { %>
        <li>
          <div class="task-text">
            <strong style="<%= todo.completed ? 'text-decoration: line-through;' : '' %>"><%= todo.task %></strong><br/>
            <% if (todo.dueDate) { %>
              <span class="due-date">Due: <%= new Date(todo.dueDate).toLocaleDateString() %></span>
            <% } %>
          </div>
          <div class="actions">
            <form action="/todos/toggle/<%= todo._id %>" method="POST">
              <button type="submit"><%= todo.completed ? 'Undo' : '✓' %></button>
            </form>
            <form action="/todos/edit/<%= todo._id %>" method="GET">
              <button type="submit">✏️</button>
            </form>
            <form action="/todos/delete/<%= todo._id %>" method="POST">
              <button type="submit">🗑️</button>
            </form>
          </div>
        </li>
      <% }) %>
    </ul>
  </div>

  <footer>Created by Rishabh Roy</footer>
</body>
</html>



